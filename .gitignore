node_modules
dist
.DS_Store
.env
.env.local

# Local-only: Allen's private compositions + assets.
private/

# Build / log noise.
coverage/
pnpm-debug.log*

# Render outputs (kept local, not committed).
out
